<div class="card">
    <div class="card-content">
        <span class="card-title">{{homeTeam.name}} vs {{awayTeam.name}}</span>
        <p>Matchday: {{matchday}}</p>
        <p>Status: {{status}}</p>
        <!-- Add more data as you like -->
    </div>
</div>
